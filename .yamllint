extends: default

ignore: |
  .travis.yml
  cookiecutter/**/buildspec.yml
  docker-compose.yml

rules:
  braces:
    max-spaces-inside: 1
    level: error
  brackets:
    max-spaces-inside: 1
    level: error
